function add(num1,last){
    const result = num1 + last;
    console.log(num1,last,result)
    return result;
}

const first = 'jaan';
const last = 'pakhi'

// 

const a = 10;
const b = 20;
const result = 'the sum of number is ' + a + ' and '+ b + ' is ' + (a + b) 
// console.log(result);

const numbers = [10,20,3,0,4];
const students= {
    name:'ratul',
    age : 17
}
const math = `The sum of number is ${numbers[1]} and ${students.age} is ${numbers[1] + students.age}`
console.log(math)


const email = `My name is ratul
javascript output can show 
this code can you use the 
console.log(email)`

// console.log(email)